import Investment from'../models/Investment.js';import User from'../models/User.js';import Withdrawal from'../models/Withdrawal.js';export const stats=async(req,res)=>res.json({users:await User.countDocuments({role:'user'}),pendingPayments:await Investment.countDocuments({status:'pending'}),pendingWithdrawals:await Withdrawal.countDocuments({status:'pending'})});export const users=async(req,res)=>res.json(await User.find().select('-password').sort('-createdAt'));export const payments=async(req,res)=>res.json(await Investment.find().populate('user package').sort('-createdAt'));export const approvePayment=async(req,res)=>{const inv=await Investment.findByIdAndUpdate(req.params.id,{status:'approved',startDate:new Date()},{new:true}).populate('user');if(inv.user.referredBy)await User.findOneAndUpdate({referralCode:inv.user.referredBy},{$inc:{referralWallet:50,wallet:50}});res.json(inv)};export const rejectPayment=async(req,res)=>res.json(await Investment.findByIdAndUpdate(req.params.id,{status:'rejected'},{new:true}));export const withdrawals=async(req,res)=>res.json(await Withdrawal.find().populate('user').sort('-createdAt'));export const setWithdrawalStatus=async(req,res)=>res.json(await Withdrawal.findByIdAndUpdate(req.params.id,{status:req.body.status},{new:true}));export const toggleBlock=async(req,res)=>{const u=await User.findById(req.params.id);u.blocked=!u.blocked;await u.save();res.json(u)};
