import express from'express';import{protect,adminOnly}from'../middleware/auth.js';import * as a from'../controllers/adminController.js';const r=express.Router();r.use(protect,adminOnly);r.get('/stats',a.stats);r.get('/users',a.users);r.patch('/users/:id/block',a.toggleBlock);r.get('/payments',a.payments);r.patch('/payments/:id/approve',a.approvePayment);r.patch('/payments/:id/reject',a.rejectPayment);r.get('/withdrawals',a.withdrawals);r.patch('/withdrawals/:id',a.setWithdrawalStatus);export default r;
